@model IEnumerable<ProductDto>

@foreach (var p in Model ?? Enumerable.Empty<ProductDto>())
{
    <div>
        <h3>@p.Name</h3>
        @p.Description
        <h4>@p.Price.ToString("c")</h4>
    </div>
}


